import Head from "next/head";
import Link from "next/link";
import { format, parseISO } from "date-fns";
import { getAllSnippets } from '../lib/data'

export default function Home({ snippets }) {
  return (
    <div>
      <Head>
        <title>100 code solutions</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <div className="space-y-4">
        {snippets.map(item => (
          <CodeInspirationItem key={item.slug} {...item} />
        ))}
      </div>
    </div>
  );
}

export async function getStaticProps() {
  const allSnippets = getAllSnippets();

  return {
    props: {
      snippets: allSnippets.map(({ data, content, slug }) => ({
        ...data,
        description: data.description,
        date: data.date.toISOString(),
        content,
        slug
      })),
    },
  };
}

function CodeInspirationItem({ slug, title, date, description }) {
  return (
    <div
      className="border border-gray-200 shadow hover:shadow-lg hover:border-gray-300 rounded-md p-4 transition duration-200 ease-in"
      key={slug}
    >
      <div>
        <Link href={`/blog/${slug}`}>
          <a className="font-bold text-xl">{title}</a>
        </Link>
      </div>
      <div class="text-gray-500 text-sm">
        <p>{format(parseISO(date), "do MMMM, uuu")}</p>
      </div>
      <p class="font-semibold text-gray-500 text-lg">{description}</p>
    </div>
  );
}
